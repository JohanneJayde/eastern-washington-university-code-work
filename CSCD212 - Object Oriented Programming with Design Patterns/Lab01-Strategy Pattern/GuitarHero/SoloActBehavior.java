/*Name: Johanne McClenahn
 *Class: CSCD212
 *Description: SoloActBehavior serves as an interface that is used when creating a new solo act.
 *An abstract method is used in order for each solo act to have their own message displayed when a GameCharacter uses them
 *
 */

//interface: SoloActBehavior
public interface SoloActBehavior {
	
	//Abstract method soloAct() used for displaying each each guitar
	public void soloAct();
}
